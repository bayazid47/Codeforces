#include <bits/stdc++.h>

using namespace std;

int main(){
	int t;
	cin >> t;
	while(t--){
		int n, k;
		cin >> n >> k;
		bool yes=0;
		for(int i=0; i< n; i++){
			int a;
			cin >> a;
			if(a==k)yes=1;
		}
		if(yes)cout << "YES"<<endl;
		else cout << "NO"<<endl;
	}
}
